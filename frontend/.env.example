# Copy this file to .env or .env.local and edit values for your local dev
# For Create React App, variables must start with REACT_APP_
REACT_APP_API_URL=http://localhost:8080
# Optional: you can set a separate port and let api.js build the URL
# REACT_APP_PORT=8080
